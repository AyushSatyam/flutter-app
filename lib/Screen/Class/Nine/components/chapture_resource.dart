import 'package:flutter/material.dart';

class ChaptureResource extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Center(
          child: Text("This is Resources section"),
        ),
      ),
    );
  }
}
