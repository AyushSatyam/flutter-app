import 'package:flutter/material.dart';

class ChaptureExam extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is exam section"),
      ),
    );
  }
}
